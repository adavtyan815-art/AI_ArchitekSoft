node_modules/
.next/
out/
dist/
data/*.db
data/*.db-*
data/uploads/
data/tmp/
# the owner's local drop folder (their own files, plus _imported/ and _failed/)
data/inbox/
# data/audio/ ships with a curated CC0 starter pack (see data/audio/SOURCES.txt); anything the
# owner drops in on top of that stays local, like the rest of this section.
data/audio/*
!data/audio/README.txt
!data/audio/SOURCES.txt
!data/audio/01_Luxury_Modern_Ambient.mp3
!data/audio/02_Warm_Acoustic_Lounge.mp3
!data/audio/03_Cinematic_Minimal.mp3
!data/audio/04_Elegant_Piano_Lounge.mp3
.env
.env.local
.env.production
*.log
.DS_Store
Thumbs.db
tsconfig.tsbuildinfo
next-env.d.ts

# The repository-root .gitignore ignores package-lock.json; this project commits its lockfile
# so that `npm ci` (Docker build, CI) is reproducible.
!package-lock.json
# output of `npm run build:worker` (bundled standalone worker for the Docker image)
/worker.js
